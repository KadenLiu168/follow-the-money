#!/usr/bin/env bash
# Minimal internal Feed entry for Agent/Skill use. Forwards arguments and
# exit codes to the deterministic feed pipeline; there is no public CLI.
set -euo pipefail
exec uv run python -m follow_the_money.feed.cli "$@"
