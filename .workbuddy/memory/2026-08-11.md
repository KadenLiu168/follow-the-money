# 2026-08-11 工作日志

## follow-the-money 仓库实现（openspec change: implement-follow-the-money-repository）

完成了 112/112 个任务的 OpenSpec change 实现，从空仓库构建完整 Python 量化金融智能系统。

### 交付物
- **配置层**：`config/config.yaml`（v1 默认值）+ `config/providers.yaml`（10 个 provider + 六组强制覆盖矩阵），严格 closed YAML 校验（lone surrogate 拒绝、枚举闭合、权重和=100、未验证 adapter 禁止启用）
- **Schema 层**：11 个 JSON Schema 2020-12（feed/event/analysis/brief/degraded/4 个 LLM 输出/verified-event-packet/run-manifest）
- **Provider 运行时**：URL 校验（source_link_hosts 策略、密钥泄漏扫描）、持久速率状态注册表（initializing→active 恢复、24h 崩溃冷却、墙钟回拨不补满）、Fed/BLS/SEC/Yahoo 适配器 + 10 个契约 manifest
- **确定性引擎**：mention 级候选图（48h/0.45、12h/0.85 边界）、Evidence Ledger、canonical event 构造、Decimal 公式契约（precision-50 隔离上下文）、惊喜/置信度
- **打分与选择**：30/20/20/20/10 权重、Morning Relevance 40/25/20/15、0.70/0.30 Brief Priority、15 分 story-family 惩罚、12 硬上限
- **Brief 链路**：4 个 LLM pass（resolver/analyst/editor/audit）prompts + 单模型适配器（重试矩阵、reasoning 校验）、确定性中文 Markdown 渲染器（实体/反引号/链接转义）、claim 审计（交易指令词典）、原子 run bundle + replay
- **评估**：30 天 golden dataset（8 类场景全覆盖）、5 个指标 oracle、ranking stability、离线门禁、live 预算控制器
- **工作流**：`test.yml`（托管 CI，免凭据）+ `generate-feed.yml`（模板，专用 runner/持久化标记 gate 默认 false）

### 关键决策
- 所有 provider 保持 `enabled: false, verified: false`（任务 3.17/3.18 语义：未验证 adapter 不得计数），strict enablement 校验在适配器真实验证前保持失败（设计如此，非 bug）
- 468 个 pytest 全部通过；`openspec validate --strict` 通过；offline eval 验证 30 天数据集

### 环境注意
- uv 0.11.21；dev 依赖必须用 `[dependency-groups]`（非 `[project.optional-dependencies]`），否则 `uv sync` 不装 pytest
- `uv sync --frozen --all-groups` 后用 `uv run pytest`
