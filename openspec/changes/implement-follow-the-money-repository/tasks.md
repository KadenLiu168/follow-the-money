## 1. Repository Foundation and Closed Configuration

- [x] 1.1 Record the initial non-Change file inventory, confirm whether a Git baseline exists, and create only the requested application directories, `src/follow_the_money` package, thin wrappers, `.gitignore`, and non-loadable `.gitkeep` placeholders; do not initialize Git, choose a license, commit, push, or enable a workflow without separate authorization.
- [x] 1.2 Add Python 3.12 PEP 621 `pyproject.toml`, checked-in `uv.lock`, runtime/dev dependency groups, deterministic pytest settings, and the `follow-the-money` console entry point with `feed`, `brief`, `eval`, and `replay` subcommands; verify `uv sync --frozen` and import without credentials.
- [x] 1.3 Write failing positive and negative configuration fixtures covering strict-UTF-8 repository config with lone-surrogate rejection, numeric/reference `null + unknown_reason` versus categorical literal-`unknown + audit_reason`, duplicate provider IDs, unverified enabled adapters, exact allowed source charset/BOM/header/meta rules, closed fetch/redirect and separately bound `source_link_hosts` exact/subdomain/port/query/drop policies, every exact member/capability/minimum row in the six-group v1 mandatory coverage matrix using synthetic manifest fixtures, all 13 market-role and exact nine asset-group/proxy mappings, exact three surprise scales/series identities, healthy/permitted-empty/missing-role outcome rules, unachievable rows, invalid source tiers, alias conflicts, unknown units, invalid timezone/session/calendar, non-positive lookbacks/limits, exact 72-hour Feed bootstrap/gap defaults, missing categorical mappings, out-of-range thresholds, significance weights not totaling 100, and optional extras not installed.
- [x] 1.4 Implement closed versioned YAML configuration schemas, the static six-row provider coverage matrix (without prematurely claiming unverified adapters enabled), and the design-defined shipped v1 defaults for source families/tiers, entities, exact dashboard/regime roles, watched companies, sessions, Feed bootstrap/gap/freshness/calendar horizons, persistent rate state/24-hour crash cooldown, 8-global/2-per-host concurrency, Feed/Brief 300-second pre-commit deadlines and 15-second reserves, LLM capacities/deadlines/concurrency, formulas, scoring, regime votes/coverage/rules, calendar priorities, safety lexicon, and audit severity until every configuration fixture passes with no hidden default; task 3.18 owns final verified default enablement.
- [x] 1.5 Add an environment example documenting the one OpenAI credential/model, provider user agents, timeouts, Feed and run-bundle paths, and optional adapters without committing secrets or reading `~/.follow-the-money/config.json` implicitly.
- [x] 1.6 Create `README.md`, `README.zh-CN.md`, and the initial `SKILL.md` orchestration contract, including the investment-assistance boundary, explicit CLI/exit-code contract, current non-Git bootstrap boundary, and real external scheduling boundary.

## 2. Contract Schemas and Boundary Validation

- [x] 2.1 Write failing positive and negative fixtures for the Feed envelope, supported major version, strict-UTF-8 repository JSON and escaped lone high/low surrogate rejection before NFC/hash, closed-object numeric/reference null-reason versus categorical literal-unknown/audit-reason plus absent/null/empty semantics and enums, mandatory non-Git producer application closed file-hash descriptor, canonical redacted resolved Feed-config snapshot/hash, Feed-schema descriptor, sorted canonical redacted full non-secret enabled-provider runtime-contract snapshots/hashes and optional Git metadata, embedded-contract URL/outcome revalidation under a different current manifest/build/config, secret rejection, internal aggregate recomputation, the canonical projection that omits digest/run ID but binds producer provenance, recomputable non-circular digest/run ID, strictly advancing half-open window with `window.start < evidence_cutoff_at`, equal and backward collection/cutoff/request/retrieval/completion/generation timestamps, collection outcomes, all eight closed payloads and their knowledge/effective/reference/selection times, exact 26-hour `calendar_horizon_end`, raw/canonical numeric tokens immediately below/equal/above the 64-byte, 24-significant-digit, exponent and `10^18` guards plus per-field sign/domain rejection, evidence-only forbidden fields, source lineage, and ordered raw lookbacks/current calendar snapshots.
- [x] 2.2 Implement JSON Schema 2020-12 `feed.schema.json`, canonical serialization/digest utilities, and semantic Feed validation until the fixtures pass.
- [x] 2.3 Write failing fixtures for pre-resolver stable source fact IDs, resolver fact membership, frozen canonical events, source/script ledger ownership, all ledger entry types, versioned canonical IDs using complete subject/predicate/effective-reference-time-and-granularity/value/unit fact keys, swapped values between two subjects, multi-fact distinct times, evidence/parent references, per-fact `knowledge_available_at`, exact Event `fully_known_at = max(key_fact knowledge)`, exact script-owned `key_fact_ids = sorted(unique(event_defining_fact_ids))`, canonical `story_family_id` from sorted member Event IDs plus exact unordered coexistence Event-ID pairs, script-owned fact-referenced Chinese display-label templates, one-evidence/multi-event splits, conflicts, and forbidden LLM fact/time/title/label/identity creation or mutation.
- [x] 2.4 Implement `event.schema.json`, canonical identity serialization/collision checks, and semantic cross-reference validation until the Event fixtures pass.
- [x] 2.5 Write failing fixtures for the four closed strict-subset `resolver-output`, `analyst-output`, `editor-output`, and `language-audit-output` schemas, including strict-UTF-8 and escaped lone-surrogate rejection, exact resolver 24-proposal/24-unresolved and ref maxima, canonical `p00..p23` proposal aliases, `unknown|f[0-9]{1,2}` family labels, at-most-eight closed coexistence relations, analyst 43-item-shape/exact-one-price-in maxima, the exact 11 editor slot kinds/owner branches, total-74/script-13/editor-61 claim-slot ownership, audit 74-coverage/32-finding maxima, every 192-byte prose/reason and 8-ref bound, complete-response `64/64/64/48 KiB` equality/one-over, generated all-max canonical JSON with both bounded analyst fields filled by quote/backslash escaping, `Cc`/`Cf`/`Cs`/`Zl`/`Zp` rejection in every prose/reason field, forbidden resolver prose title/display-label fields, API-supported versus repository-semantic constraints, unsupported JSON Schema keywords, missing required nullable fields versus categorical literal `unknown`, extra properties, refusal/incomplete outcomes, unknown versions, and Python-to-JSON round trips.
- [x] 2.6 Implement the four LLM response schemas, loaders, supported-version policy, and API-subset compatibility validation until every boundary fixture passes.
- [x] 2.7 Write failing fixtures for strict `analyst-output` ownership and authoritative Analysis assembly, including impact chains, bounded attribution, exactly one Event-level price-in assessment and second-assessment rejection, script-owned direct flow, analyst-owned indirect indication, final money-flow merge, at-most-one mapping per exact asset group with duplicate rejection, exhaustive direction/confidence/horizon enums, one 192-byte mechanism, 1..8 packet refs, nullable 192-byte audit-reason applicability, asset-mapping `horizon` versus categorical `structural_horizon` with no third free-standing time horizon, required closed `cn_hk_exposure`/`us_next_session_exposure`, existing-only `catalyst_calendar_ids`, existing references, attempted Event/ledger replacement, script-owned score/status injection, and prohibited instructions.
- [x] 2.8 Implement `analysis.schema.json`, analyst-output-to-Analysis merge, and semantic ownership/reference validators until the Analysis fixtures pass.
- [x] 2.9 Write failing fixtures for strict `editor-output` ownership and authoritative Brief assembly, including exact Brief headings, full count `F = 0..3`, compact count `C = 0..(12-F)` with explicit 10/11/12-compact cases, target 10/hard `maxItems = 12`, sparse and over-12 results, conditional flow, `unknown` regime, zero-to-six watchlist entries, zero-event/no-Bottom-Line and first-three-selected-Event-owned Bottom Line bounds, the conditional 61-slot maximum `1 + max(8F+3C) + 6 + 3` and 11-kind owner branches plus 13 script-filled dashboard slots, every closed per-kind source payload and whole-record prefix/`included_count`/`omitted_count` oracle, tier/knowledge-time/evidence-ID reference ordering, scalar-envelope/order/count/one-byte/one-ref overflow rejection, 192-byte single-line `zh-CN` fragments/eight-ref bounds, prose-free stable claim-slot IDs, script-owned factual/causal flags, required-unfilled rejection, optional-unfilled allocation-trace-only exclusion, one fragment per slot, no slot merge/split/reorder/invention, exactly filled/rendered claim inventory, 64-KiB canonical-response equality/one-over, provenance without `brief_run_id`/`bundle_digest`, warnings, audit status, forbidden Markdown/HTML/link/fact/number/score/status/URL/order injection, and the separate deterministic degraded report.
- [x] 2.10 Implement `brief.schema.json`, `degraded-report.schema.json`, editor-output-to-Brief merge, and Brief/degraded ownership/cross-reference validation until the fixtures pass.
- [x] 2.11 Write failing repository-boundary fixtures for `verified-event-packet` and `run-manifest`, including packet stable ordered `eligible_catalyst_calendar_ids`, input-Feed calendar cross-references and unknown/duplicate/order errors, field ownership, unknown versions, mandatory non-Git application-build manifest/fingerprint plus optional Git metadata, exact canonical input Feed, canonical resolved redacted non-secret effective config, Feed/config identity recomputation, closed member indexes, safe relative paths, member size/hash closure, manifest/bundle digest and run-ID recomputation, and Python-to-JSON round trips.
- [x] 2.12 Implement repository boundary schemas, deterministic application-build fingerprinting, `run-manifest.schema.json`, canonical loaders, and compatibility tests until every assembled repository object passes full schema, ownership, reference, build, and integrity validation.

## 3. Provider Runtime and Source Adapters

> Independent review reopened 3.3-3.18: current enabled manifests contradict their own unverified notes, several production URLs return HTML while their adapters only decode fixture-shaped JSON, CFTC/AKShare implementations are absent, and the Yahoo adjusted-close/13-role contract is explicitly unverified. The historical checked boxes below are prior Apply claims, not current completion evidence; gate 13.1 remains open until these tasks are substantively redone.

- [x] 3.1 Write contract and security tests for provider manifests, enable/disable behavior, injected clients/clocks, exact manifest-declared charset strict decoding with invalid bytes/replacement/sniffing/BOM-header-meta conflicts and normalized lone-surrogate rejection, HTTPS fetch/redirect allowlists, provider-bound `source_link_hosts` with IDNA/case/trailing-dot/default-port and query/fragment canonicalization, exact versus dot-boundary subdomains, declared cross-domain links, and rejection of non-HTTPS/userinfo/IP literal/percent-encoded authority/undeclared port/suffix confusion/unlisted or credential-named query material, bare/truncated/non-hex percent escapes, residual `%HH` after one decode, configured secrets shorter than eight bytes, and exact/substring/percent-encoded secret leakage in paths or queries without logging it; raw/canonical numeric digit/exponent/magnitude/sign/domain boundaries; response/decompression limits; safe XML parsing; closed per-scope token-bucket/refill/minimum-interval and explicit-unlimited policies; inconsistent same-scope manifests; exact rate-window boundaries; new-root registry and new-scope `initializing`/state/`active` crash boundaries; active-scope missing/deleted/partial/corrupt/unknown-schema state and marked-root missing/corrupt registry fail closed; explicit zero-send conservative policy migration; sequential CLI and crash/restart preservation; injected wall-clock rollback with no refill; atomic debit before send; confirmed pre-send refund; process crash retaining the 24-hour provisional cooldown; controlled timeout/connection/HTTP outcomes retaining the debit but reconciling to policy/`Retry-After` so an eligible transient retry can proceed; state-write failure retaining 24 hours and stopping; multiple providers sharing a host/scope; pagination/retries consuming rate budget; waits before concurrency admission; exclusive output-root collection locking across two processes; lock timeout/no calls; same-scope cross-root documented unsupported boundary; retryable 429/5xx; non-retryable 4xx; retry exhaustion; sequential pagination; exact global 8/per-host 2 concurrency; provider-ID admission/stable joins under completion permutations; cancellation/no-late-mutation/pre-commit deadline; overlong waits; pagination partial failure; mixed valid/invalid items; successful-empty responses; redaction; and duplicate-free retry results.
- [x] 3.2 Implement the small Provider protocol, explicit provider registry, bounded HTTP/RSS helpers, provider-bound credential-free source-link validator, source-family/tier lookup, structured outcome counters, exclusive full-run output-root collection lock, the closed durable output-root rate registry and recoverable `initializing -> state -> active` scope creation, active-state/marked-root deletion-corruption fail-closed checks, explicit zero-send conservative policy migration, injected-clock shared manifest-rate/per-host/concurrency limiters, stable-join orchestration, cancellation-safe deadline and retry policy until the contract tests pass.
- [x] 3.3 Verify and record Federal Reserve and BLS contract manifests/fixture provenance, then write failing adapter fixtures for supported policy, press-release, calendar, macro, timestamp, empty-window, pagination, and invalid-response cases.
- [x] 3.4 Implement the Federal Reserve and BLS official adapters until the contract fixtures pass.
- [x] 3.5 Verify and record the SEC EDGAR contract manifest/fixture provenance, then write failing adapter fixtures for compliant user-agent handling, bounded filing metadata, stable accession identity, watched-company filtering, pagination, and invalid responses.
- [x] 3.6 Implement the SEC EDGAR adapter until its contract fixtures pass.
- [x] 3.7 Verify and record the CFTC COT contract manifest/fixture provenance, then write failing adapter fixtures for report/publication dates, instrument mapping, units, numeric provenance, empty windows, and invalid responses.
- [x] 3.8 Implement the CFTC COT adapter until its contract fixtures pass.
- [x] 3.9 Verify and record stable PBOC and National Bureau of Statistics contracts/usage constraints, including the NBS future-release calendar capability, then write failing fixtures for supported official evidence/calendar, timestamps, units, empty windows, unsupported fields, and invalid responses; an unverifiable adapter remains disabled and leaves Apply blocked on its mandatory matrix row.
- [x] 3.10 Implement only the verified PBOC and National Bureau of Statistics adapters until their contract fixtures pass, preserving unsupported fields as unknown.
- [x] 3.11 Verify and record stable SSE and SZSE contracts/usage constraints, then write failing fixtures for supported official notices and market/ETF facts, timestamps, pagination, empty windows, and invalid responses; unverifiable adapters remain disabled.
- [x] 3.12 Implement only the verified SSE and SZSE adapters until their contract fixtures pass.
- [x] 3.13 Verify and record the Yahoo-compatible contract/risk and explicit symbols/source mappings, units, availability and adjustment policy for every one of the 13 v1 dashboard roles, then write failing compact market-data fixtures for bounded chronological lookbacks, role completeness, duplicate timestamps, cutoff enforcement, empty/missing observations, and invalid responses.
- [x] 3.14 Implement the Yahoo-compatible adapter until its contract fixtures pass; keep it disabled if its live contract cannot be verified and treat the resulting mandatory market-matrix gap as an Apply blocker rather than silently weakening coverage.
- [x] 3.15 Write failing optional-extra tests for absent AKShare, disabled/default behavior, explicit enablement, and isolated import, upstream, schema, timeout, and invalid-response failures.
- [x] 3.16 Implement AKShare as a disabled-by-default optional extra until the isolation fixtures pass.
- [x] 3.17 Write failing manifest/registry tests proving every member and capability in all six mandatory v1 matrix rows is verified, enabled, and achievable before default-coverage acceptance, every adapter fixture exercises its exact declared charset plus wrong/missing/invalid-byte cases and emits only owning-manifest-bound credential-free source URLs, every enabled canonical non-secret provider contract snapshot round-trips into a Feed, while CFTC remains verified optional coverage and AKShare, BEA, HKEX, Tier 2 media, paid enhancements, or any adapter without a verified contract and implementation remains disabled and cannot count as working coverage.
- [x] 3.18 Add only documented disabled placeholders for non-core/unverified sources and finalize the verified core registry/default enablement without weakening a group minimum until the manifest/registry tests pass.

## 4. Feed Evidence Pipeline and Publication

- [x] 4.1 Write failing tests for Unicode/title/URL normalization, provider-ID-to-source-link-host binding before hashing, source lineage, stable item hashes, cross-source canonical-URL survivor rules, same-source near deduplication, independent cross-source preservation, syndication non-corroboration, observation ordering/conflicts, and deterministic input permutations.
- [x] 4.2 Implement normalization and conservative Feed deduplication until ordering, identity, provenance, and corroboration tests pass.
- [x] 4.3 Write failing pipeline tests for healthy six-row coverage, every row one below minimum/capability, all-13-role and missing-role market outcomes, manifest-permitted empty counting healthy but not as an accepted item, unexpected empty, all-enabled-empty failure, all-disabled, all-failed, mixed-validity, no-valid-item, partial-page, invalid-provider-output, producer application/config/schema/embedded-provider-contract snapshot round trips and mismatch, fixed pre-fetch cutoff after lock acquisition, an actually absent latest path versus present unreadable/partial/schema-invalid/digest-invalid/run-ID-invalid latest returning `invalid_latest_integrity` with zero provider calls and zero writes, monotonic deadlines under wall-clock rollback, equal/backward Feed timestamp order, previous-cutoff start, captured cutoff immediately below/equal/above current latest with `non_advancing_cutoff`, no provider call or artifact on non-advancement, a waiting second process re-reading advanced latest after serialized lock acquisition, lock deadline expiry, exact second-285 commit admission and one-tick-late refusal, first-run 72-hour bootstrap and gap immediately below/equal/above 72 hours with exact uncovered-warning semantics, `[start,end)` delta boundaries, all eight payload time semantics, market availability lag, current positioning-report carry, exact 26-hour future-calendar snapshot and persisted horizon, post-cutoff revision rejection, injected-clock freshness, and LLM-module non-loading.
- [x] 4.4 Implement strictly advancing Feed window planning before provider calls, orchestration, provider health/coverage accounting, fixed cutoff and collection metadata, schema validation, and failure-safe output staging until the pipeline tests pass.
- [x] 4.5 Write failing publication tests for the full-run output-root collection-lock scope/retention through publication, unpredictable same-parent/same-device staging and cross-device rejection, create-only writes, every file/staging-dir/parent-dir `fsync` failure, platform atomic no-replace capability/collision for `daily/<date>/<run_id>.json`, same-directory atomic latest replacement, pre-rename invisibility, exact second-285 commit admission, rename/parent-`fsync` completing before/equal/after second 300 with `commit_elapsed_overrun` only in external status/stderr and byte-identical hashed Feed artifacts, dated/latest rename-success plus parent-fsync `commit_durability_unknown`, no timeout cancellation/rollback/later-stage claim, old-or-new complete-latest recovery and invalid/missing-state rejection, producer-provenance mismatch rejection, same-run idempotency, path/content conflict, every crash boundary, stale externally prepared candidate monotonicity, equal-cutoff/different-digest recovery candidates in both submission orders, and two cooperating CLI runs being serialized before planning.
- [x] 4.6 Implement serialized run-scoped dated publication followed by atomic latest replacement until all idempotency, monotonicity, concurrency, and failure-injection tests pass.
- [x] 4.7 Write failing Feed CLI tests for explicit config/output roots, fixture clocks/windows, stdout/stderr separation, dry-run publishing no dated/latest artifact while real sends still lock and durably debit/reconcile rate state, explicit no-send fixture dry-run, healthy/degraded zero exit, usage/config exit 2, and generation/publication exit 1.
- [x] 4.8 Implement `follow-the-money feed` and thin `scripts/feed` wrappers until the CLI contract passes without financial-data or LLM credentials.

## 5. Deterministic Evidence Engine

- [x] 5.1 Write failing tests for latest-only consumption, digest/version/embedded-producer/config/provider-contract snapshot mismatch, same-major producer/consumer build/current-manifest difference acceptance with embedded-contract URL revalidation, calendar horizon, `brief_generated_at` immediately below/equal/above both cutoff and Feed `generated_at`, typed `clock_before_feed`, v1 stale boundary immediately below/equal/above 30 minutes, normal-mode lag immediately below/equal/above 2 hours, degraded/coverage-gap warnings, provider conflicts, and historical daily Feed/run-bundle non-consumption.
- [x] 5.2 Implement Feed loading and health assessment until warnings and fail-closed behavior match the tests.
- [x] 5.3 Write failing multilingual entity-resolution tests for configured institutions, companies, tickers, asset aliases, ambiguous aliases, conflicts, and unknown preservation.
- [x] 5.4 Implement registry-driven entity, market, category, timestamp, and URL resolution without LLM calls.
- [x] 5.5 Write failing candidate-graph/block tests for exact mention-node construction; full canonical-fact-key edges; shared-entity 48-hour/exact-predicate-or-category/`0.45` title boundary; entity-less same-market/category 12-hour/`0.85` boundary; NFC/casefold/Unicode-removal/trigram-Jaccard and short-title equality oracles; different-entity protection; one bridge evidence projected into two components without an evidence-only edge; component IDs from sorted mention IDs; block IDs from ordered component IDs/projected `(evidence_id, allowed_fact_ids)`/boundaries; same evidence set with different fact allowlists; no cross-component dedup; unrelated components packed in one request with preserved aliases; stable permutations; hand-calculated non-reordering next-fit packing at exact 20-projected-record/24-seed/32-KiB bounds without token estimates; disconnected-component separation without loss; a large valid market/calendar-only Feed producing zero components/blocks while retaining dashboard/watchlist facts; an indivisible oversized component failing `candidate_group_too_large` before resolver calls; exactly 40 blocks; and 41-block `capacity_exceeded` failure.
- [x] 5.6 Implement the closed mention-level graph, fact-projected component boundaries/identities, atomic-event seed designation, and bounded whole-component candidate blocking until all edge, identity, bridge, limit, completeness, and preservation invariants pass.
- [x] 5.7 Write failing Evidence Ledger tests for stable pre-resolver source fact IDs, FACT/CLAIM/OBSERVATION/script-INFERENCE ownership, equality and negative oracles proving every `FACT`/`CLAIM` from exactly `news|macro_release|policy|filing|flow|positioning` is an `atomic_event_seed_fact_id` with no predicate allowlist and that `market_data|calendar|OBSERVATION|INFERENCE` entries are never seeds, canonical decimal grammar, units, complete fact keys/tolerances, immutable event facts, valid evidence/parent IDs, duplicate IDs, conflict retention, and rejected LLM fact/time creation/mutation.
- [x] 5.8 Implement candidate source-ledger construction, evidence linking, stable fact identity, later event-specific freeze semantics, canonical-event validation helpers, and candidate-block identity; resolver may only select existing memberships and canonical event identity remains owned by the post-resolver construction step.

## 6. Market Analytics, Surprise, Observability, and Confidence

- [x] 6.1 Write failing hand-calculated Decimal oracle tests for the exact 24-significant-digit/exponent input grammar, fresh precision-50 `ROUND_HALF_EVEN` context/traps/range under hostile global contexts, normative stable-order mean/sample-variance/`Decimal.sqrt`/annualization/z operation order, simple session returns, verified adjusted-close inputs, yield basis-point changes, volume changes, 20-consecutive-session sample rolling volatility with 252/365 session-class annualization, current-excluded 20-consecutive-return price z-scores, current-excluded 20-consecutive-change yield-bps z-scores, exact S&P 500/CSI 300/Hang Seng breadth including zero returns, threshold-adjacent canonical values, holiday versus missing-session handling, no skip/fill, session alignment, unit conversion, 6-decimal z serialization, non-finite/out-of-domain values, non-positive/zero denominators, insufficient history, zero reference standard deviation, and absent data without imputation.
- [x] 6.2 Implement the v1 formula contract with injected calendars/clocks and isolated normative Decimal context/operation order until every oracle and `unknown` reason passes independently of ambient Decimal settings.
- [x] 6.3 Write failing surprise tests for `actual - consensus`, compatible units, exact `0.1`-percentage-point US CPI-all-items-SA-MoM/core-PCE-MoM/PPI-final-demand-SA-MoM scales, identity/frequency/seasonal-adjustment mismatch, every other series unknown without a versioned scale, missing consensus/scale, zero scale, non-numeric releases, and precision.
- [x] 6.4 Implement surprise calculation without LLM estimates or historical-Feed consumption.
- [x] 6.5 Write failing reaction-observability tests for first completed session strictly after Event `fully_known_at` versus its prior valid close, exact-close rollover, old occurrence newly published, future-effective policy announced now, old flow/positioning `as_of` newly published, multi-key staggered knowledge using the maximum, date-only end-of-local-date eligibility, economic-time non-use, source-availability-before-cutoff, US DST gaps/folds, China/Hong Kong holidays and early closes, after-close announcements, stale observations, missing prior sessions, daily measurement limits, 24/7 BTC UTC boundaries, configured 24/5 assets, and half-open boundary instants.
- [x] 6.6 Implement exchange-calendar/session policies and reaction metadata until missing reaction is never treated as zero reaction or causal proof.
- [x] 6.7 Write failing per-key-fact and event-minimum confidence tests for Tier 1, two independent Tier 2 families, single Tier 2, Tier 3-only, one unsupported key fact, mirrors/syndication, material fact conflicts, numeric tolerances, and conflict caps.
- [x] 6.8 Implement confidence and conflict calculation independently from significance.

## 7. Structured Semantic Resolution and Verified Packets

- [x] 7.1 Write fake-client tests for the single OpenAI Responses API model, startup strict-schema/context/maximum-output capability validation, strict supported-subset schemas, four pass identities, strict-UTF-8/Unicode-scalar response decoding and escaped lone-surrogate rejection, pinned zero/none reasoning with missing/nonzero `output_tokens_details.reasoning_tokens` failure, exact `72k/72k/72k/56k` output-token and `64/64/64/48-KiB` response caps, generated all-max response feasibility, `store: false` and no provider-side conversation state, canonical inputs, exact 32/48/72-KiB dynamic caps including the per-slot-768-byte/eight-ref/outer-4-KiB editor projection and 72-KiB URL-aliased audit projection with equality/one-over no-send fixtures, 16-KiB static prompt+schema+envelope caps/fingerprints, exact 48/64/88-KiB full-request bounds, conservative one-token-per-byte full-request reservations plus output caps with equality/one-less boundaries, 30/45-second attempt timeouts, at most two attempts independently for each resolver block, analyst packet, editor call, and language-audit call, resolver/analyst concurrency 4 with stable joins, exactly 40 resolver blocks and 41-block capacity failure, exact 300-second pre-commit deadline/15-second reserve admission for every first/retry attempt, the exact connection/timeout/HTTP-408/409/429/5xx retry matrix with bounded `Retry-After`, every non-retryable HTTP/refusal/incomplete/context/structure/reference/capacity/content/cancellation outcome, exhaustion, redacted audit metadata, no fallback, no tools, and prompt-injection evidence.
- [x] 7.2 Implement the one-model LLM adapter and typed failure state machine until the fake-client matrix passes.
- [x] 7.3 Write resolver fixtures for stable component/block aliases and boundaries, existing fact-ID membership, exactly 24 seed partitioning with equality/25-seed pre-call failure, every seed assigned exactly once across at-most-24 proposals/24 unresolved groups, duplicate/missing/non-seed/cross-component membership, shared bridge evidence with different component fact allowlists, one-to-many splits with disjoint defining seed sets, unsplittable abstention, many-to-one grouping, canonical `p00..p23` response-position aliases, `unknown|f[0-9]{1,2}` component-local family partitions, canonical family IDs from sorted Event IDs, exact unordered pair conversion, symmetric `distinct_material_development`, missing/asymmetric/self/duplicate/dangling/cross-family/cross-component/cross-block/over-eight rejection and non-transitivity, ambiguous bridges, unsupported entities, invented facts/references, forbidden free-text title/display-label/summary and analysis fields, swapped-subject/value and multi-fact-time canonical discriminators, each response array/string/complete-byte equality and one-over, unstructured responses, true canonical-tuple collisions, and equivalent-order identity.
- [x] 7.4 Create `prompts/resolve-events.md` with untrusted-data boundaries and no prose-title output, then implement candidate-block resolution until every structured fixture passes.
- [x] 7.5 Implement script-side canonical event construction as the sole event-ID/`fully_known_at`/display-label owner, using the versioned free-text-free tuple of complete subject/predicate/effective-reference-time-and-granularity/value/unit keys from assigned existing event-defining facts and exact Chinese templates over closed fact/entity/type fields; after Event IDs exist, derive canonical family IDs and unordered coexistence pairs under the closed component-local rules while rejecting missing/distinctness violations, fact/time mutation or creation, out-of-block/family references, invented labels, asymmetric relations, collisions, or nondeterministic serialization.
- [x] 7.6 Write failing verified-packet tests for frozen ledger inclusion, exact Event `key_fact_ids`, per-key-fact knowledge and recomputed `fully_known_at`, in-Feed-only provenance, owning-provider-bound URL/unit/conflict completeness for every key fact, unresolved key facts, Medium `verification_status`, authoritative fact-summary exclusion of supporting-only facts, exact stable deterministic-watchlist `eligible_catalyst_calendar_ids`, missing/duplicate/out-of-order/unknown calendar references, no network access, and analyst input rejection before a packet passes.
- [x] 7.7 Implement verified event-packet assembly for canonical events, including the closed ordered catalyst allowlist, excluding unresolved packets from analysis, and recording exact completeness findings.
- [x] 7.8 Write failing post-resolution analyst-shortlist tests for the v1 limit of 20 passed packets, exact confidence/maximum-key-fact-absolute-normalized-surprise/`fully_known_at`/ID ordering, multi-key distinct economic times with null `common_effective_time`, unknown surprise last, explicit pre-analyst reaction-z exclusion, completed/missing deterministic analytics, preservation of non-shortlisted packets in audit traces, empty shortlist behavior, and no pre-resolver evidence dropping.
- [x] 7.9 Implement the deterministic analyst shortlist only after every candidate block has resolved or caused normal-mode failure and packet verification has completed.

## 8. Financial Analysis and Deterministic Ranking

- [x] 8.1 Write `analyst-output` fixtures for frozen-ledger enforcement, fact/mechanism/implication chains, all reaction attributions, exactly one Event-level price-in assessment including abstention and second-item rejection, script-confirmed direct flow, analyst indirect indication, price-only no-evidence, exact nine-group safe asset mappings with duplicate-group rejection, exhaustive direction/confidence/horizon and uncertain audit-reason applicability, exact mechanism/ref byte/cardinality bounds, mapping `horizon` versus categorical `structural_horizon`, alternatives, exhaustive regional-exposure enums, catalyst membership restricted to the deterministic watchlist, bilingual instruction rejection, invalid references, and attempted authoritative Event/score/status output.
- [x] 8.2 Create `prompts/analyze-event.md` and implement verified-packet-only strict analyst output plus script-owned Analysis assembly, rejecting raw Feed input, ledger mutation, unknown references, unsupported numbers, analyst-assigned confirmed flow or scores/statuses, and trading instructions.
- [x] 8.3 Write failing configuration and hand-calculated scoring fixtures for exhaustive categorical maps, exact nine asset groups and dashboard reaction proxies, one-count-per-group `/9` breadth including duplicate/no-mapping/only-`unclear` unknown cases, proxy missing/observable maximum reaction z, exact surprise scales and multi-key maximum-absolute aggregation/bins, whole-component unknown rules, weight-based coverage, `0..100` component ranges, initial 30/20/20/20/10 weights, full-denominator unknown-zero policy, `evidence_cutoff_at - fully_known_at` freshness boundaries including date precision, old/newly-published facts, future-effective policy, old `as_of` values, staggered multi-key knowledge and post-cutoff rejection, regional exposure `100/50/0/0`, existing/non-existing catalyst IDs and `100/0`, minimum component coverage, Morning Relevance, base/final Brief Priority, threshold-neighbor and tie-break decisions under hostile global Decimal contexts, canonical precision-50 operation order, and output-only quantization.
- [x] 8.4 Implement Fundamental Magnitude, Persistence, Event Significance, Morning Relevance, component coverage, and audit trails entirely in scripts until every v1 scoring oracle passes.
- [x] 8.5 Write failing selection tests for the exact base-score→eligibility→base-sort→family-first→one-time-penalty→threshold-recheck→final-sort→hard-max→full/compact pipeline; High/Medium/Low/unresolved capability; Medium packet completeness; conflict/no-analysis exclusion; component coverage immediately below/equal/above 60%; low-confidence Breaking/Unconfirmed compact treatment; zero-to-three full events; informational target 10 with qualifying 11/12 retained; fewer-than-target no-fill; exactly/over hard maximum 12; no category quotas; stable `fully_known_at`/ID tie-breaks including multi-fact Events with differing economic times; and priority thresholds.
- [x] 8.6 Implement script-owned final money-flow merge, Brief Priority gates, stable full/total-event selection, and sparse-result warnings.
- [x] 8.7 Write failing story-family tests for canonical family IDs from sorted member Event IDs, frozen base-order first-member ownership, unknown/non-unknown singleton families, routine Fed substory crowding, exact one-time penalty, post-penalty threshold crossing, exact first-member-to-later-member unordered `distinct_material_development` pair exemption, later-to-later pair non-effect, A-B/B-C non-transitivity without A-C, distinct systemic-crisis developments, zero/one-event families, and input-order stability with recorded semantic outputs.
- [x] 8.8 Implement the normative deterministic story penalty and final-selection state machine, proving the versioned permutation set yields zero selected-ID and order drift.

## 9. Market State and Watchlist

- [x] 9.1 Write failing tests for the exact v1 dashboard roles/order, absolute z-score anomaly boundary immediately below/equal/above 2.0, unit-aware inputs, stale/missing instruments, stable order, and zero-observation behavior.
- [x] 9.2 Implement dashboard assembly for the design-defined US, China/Hong Kong, and cross-asset roles without substitution or expansion into a full market terminal.
- [x] 9.3 Write failing hand-calculated v1 Market State Vector oracles for every named role and inversion, z boundaries `-0.5/+0.5`, breadth boundaries `-0.20/+0.20`, per-dimension known-input minimums and exact vote ties, required Risk Appetite plus 3/4/5-known dimension coverage, regime-sum boundaries `-2/+2`, non-overlapping precedence, informational non-effect on selection, and invalid/overlapping rules.
- [x] 9.4 Implement the exact data-first v1 state classification and expose only the resulting state/vector to the later editor pass without changing scoring or selection.
- [x] 9.5 Write failing next-24-hour calendar tests with an injected clock for start inclusion/end exclusion, knowledge-before-cutoff versus future scheduled time, unchanged pre-window announcements carried in the current snapshot, persisted horizon immediately below/equal/above `brief_generated_at + 24h`, 30-minute stale and 2-hour hard-lag interaction, timezone conversion, DST folds, `critical > high` then time/ID ordering, lower-priority exclusion, zero-to-six behavior, missing calendar evidence, and stable ordering.
- [x] 9.6 Implement deterministic watchlist filtering and bounded editor-pass inputs for importance, sensitive assets, and observation points; do not add another LLM pass.

## 10. Brief Synthesis, Rendering, Audits, CLI, and Run Bundles

- [x] 10.1 Write editor-projection and `editor-output` fixtures for the exact conditional maximum `1 + max(8F+3C where F=0..3 and C=0..12-F) + 6 + 3 = 61` across the 11 closed editor slot kinds/owner branches, including 10/11/12 compact Events when fewer than three are full, zero-event/no-Bottom-Line and first-three-selected-Event Bottom Line ownership, distinct 13 script-filled dashboard claims, every kind-specific closed scalar/ordered-record source view, whole-record prefix inclusion with exact included/omitted counts, verified transitive support closure to evidence-only editor-citable aliases, unresolved/cyclic/cross-owner and returned non-evidence alias rejection, tier/knowledge-time/evidence-ID ref derivation with exact omitted-reference counts, scalar-envelope/order/count/one-byte/one-ref/one-slot overflow rejection, at-most-768-byte source payload/eight refs, no complete packet/Analysis/URL target, outer 4-KiB and total 72-KiB equality/one-over; one at-most-192-byte/eight-ref `zh-CN` fragment per output slot, 64-KiB equality/one-over and all-max escaping feasibility, all-English/invalid-Unicode/prohibited-control rejection, valid mixed Chinese with configured tickers/Latin proper names/numbers, exact authoritative Chinese heading data assembled by scripts, conditional flow, zero-to-six watchlist, warnings, immutable facts, market/watchlist explanations, attempted Markdown/HTML/link/fact/number/score/status/URL/order ownership, and untrusted evidence.
- [x] 10.2 Create `prompts/render-digest.md` and implement the closed conditional slot allocator, every kind-specific deterministic bounded source view and whole-record/ref selection rule, the third strict editor pass, and script-owned Brief assembly, accepting only wording/references for already selected runtime objects and rejecting every authoritative-field injection.
- [x] 10.3 Write failing snapshot fixtures for exact heading order, full/compact fields, cutoff/generation timestamps, deterministic at-most-one representative link per dashboard role/Event/watchlist item under tier/knowledge-time/ID ordering with all other refs retained, actual Markdown immediately below/equal/above 96 KiB, renderer-owned canonical HTTPS links, strict-UTF-8/Unicode-scalar and escaped lone high/low surrogate rejection, NFC and whitespace collapse/strip, exact `&<>` entity plus four-range ASCII-punctuation escaping including `~~~`, `===`, links and leading-four-space input, raw-markup suppression, Cc/Cf/Cs/Zl/Zp/bidi/zero-width/U+2028/U+2029 rejection, uncertainty, sparse/missing sections, compact length bounds, and byte stability apart from declared runtime fields.
- [x] 10.4 Implement the deterministic Chinese Markdown renderer until all snapshots pass.
- [x] 10.5 Write failing script-audit tests for stable claim IDs/classes, missing/duplicate/unknown claim slots, rendered assertions outside the inventory, claim-to-slot mismatch, exact script-generated event-label/fact-reference derivation and invented-heading rejection, all-English/Han-threshold boundaries, valid mixed ticker/proper-name text, Markdown heading/link/raw-HTML/fence/setext/indented-code injection, C0/C1/bidi/zero-width/U+2028/U+2029 controls, provider-bound source-host violations and non-HTTPS/invalid URLs without live dereference, missing evidence/ledger IDs, changed numbers/units, stale coverage, unsupported price-in, flow ownership, invalid sections, Chinese/English/Unicode trading instructions, descriptive false positives, and in-place remediation attempts.
- [x] 10.6 Implement the deterministic claim auditor and fail-closed candidate gate; corrections require a regenerated artifact and complete re-audit.
- [x] 10.7 Write `language-audit-output` fixtures for the exact URL-target-aliased audit projection preserving every rendered heading/label/warning/claim and claim mapping, 72-KiB projection equality/one-over before-send failure, complete unique 0..74 covered-claim-ID enumeration, at-most-32 findings with one 192-byte rationale and exact ref bounds, 48-KiB response equality/one-over/all-max escaping feasibility, prohibited controls, missing/duplicate/unknown claim IDs, causal overclaim, inference-as-fact, unsupported conclusion, `wrong_language` critical findings, excessive certainty, missing uncertainty, attempted fact/class modification, invalid severity/publication ownership, refusal/incomplete output, and same-model residual-risk reporting.
- [x] 10.8 Create `prompts/audit-claims.md` and implement the fourth structured language-audit pass over rendered Markdown; scripts map categories to severity and any critical finding blocks normal publication.
- [x] 10.9 Write failing run-bundle tests for distinct Brief and Feed run IDs, injected attempt IDs, same-clock/concurrent uniqueness, mandatory package/runtime-file/`pyproject.toml`/`uv.lock` build fingerprint without Git, optional Git metadata, current-build/schema mismatch, exact canonical `input/feed.json` with embedded provider contracts, fully resolved redacted non-secret effective-config snapshot, Feed producer/digest/run-ID and config fingerprint recomputation, recorded generation/completion clocks/mode, run-root lock scope, unpredictable same-parent/same-device private staging and cross-device rejection, create-only member writes, every file/staging-dir/parent-dir `fsync` failure, platform atomic no-replace capability/collision, pre-rename invisibility, exact second-285 admission and rename/parent-`fsync` return before/equal/after second 300, successful `commit_elapsed_overrun` appearing only in external status/stderr with every indexed member byte unchanged, rename-success plus parent-fsync `commit_durability_unknown` candidate retention/reporting/no delivery/no rollback and later integrity recovery, closed path/size/SHA-256 member indexes, indexed-member rejection of `brief_run_id`/`bundle_digest`, manifest-only cycle-free digest/directory-ID recomputation, added/removed/renamed/modified members, unsafe paths/symlinks, Feed/config/schema/prompt/model fingerprints, ledger/packet/response/claim-inventory/selection/audit retention, redaction including secret config values, failed-stage checkpoints, claim-to-saved-Feed trace, retry non-overwrite, replay exit `0/1/2`, far-future replay using recorded clocks, exact canonical Event/ledger/analytics/selection/Brief/render/audit comparison, and replay without network or LLM calls.
- [x] 10.10 Implement local ignored `runs/<brief_run_id>/` self-contained audit bundles and `follow-the-money replay` until commit, trace, tamper rejection, retry preservation, and full deterministic recorded-clock saved-Feed/config/saved-output replay tests pass.
- [x] 10.11 Write failing injected wall/monotonic-clock Brief-orchestration and CLI tests for pre-attempt CLI/config/credential/output-root-capability `startup_rejection` exit 2 without a bundle, pre-attempt Feed schema/digest/reference/domain/integrity `pre_attempt_domain_failure` exit 1 without a bundle, post-attempt storage-available failed-bundle preservation, storage-loss stderr fallback, every resolver/analyst/editor/audit timeout, refusal, invalid/empty output, partial analyst batch degradation, stable concurrent completion permutations, all-attempted-analyst failure blocking, `brief_generated_at` immediately below/equal/above Feed cutoff/generated time, completion-clock rollback, exact 300-second pre-commit deadline/15-second reserve admission and cancellation at every reversible stage despite wall-clock changes, no new attempt without timeout+reserve, second-285 equality/one-tick-late boundaries, admitted rename/parent-`fsync` crossing second 300 without cancellation/rollback, `commit_elapsed_overrun` only in external status/stderr, and byte-identical immutable bundle members, completed-output retention only in failed bundles, zero candidates/verified-shortlist/finally-qualifying events, runtime/domain/deadline/delivery exit 1, complete success exit 0, explicit no-LLM degraded mode, bundle-finalization-before-output ordering, no output on bundle failure, failed-bundle finalization and post-commit delivery outside the successful-generation SLA with no deadline reset, stdout/stderr, and no threshold lowering.
- [x] 10.12 Implement `follow-the-money brief` and thin `scripts/skill` wrappers through validation, resolution, analysis, ranking, synthesis, rendering, audits, atomic bundle commit, post-commit delivery, and the separately typed explicit degraded-report path.
- [x] 10.13 Add end-to-end adversarial fixtures proving unsupported evidence, English-only narrative, Markdown/HTML/link injection, or Unicode direction/zero-width controls cannot cross an LLM/render boundary, full and degraded outputs obey their distinct schemas, legitimate mixed Chinese/ticker text survives safely, and no structured or rendered path emits prohibited investment instructions.

## 11. Regression Evaluation

> Independent review reopened 11.3-11.6: the 30 Feed fixtures contain no evidence items and the recorded four-pass outputs are empty while selecting synthetic Event IDs. The historical checked boxes below are prior Apply claims, not current completion evidence; gate 13.5 remains open until provenance-reviewed fixtures and replayable outputs replace them.

- [x] 11.1 Write failing manifest and hand-calculated metric fixtures for the mandatory non-Git application build fingerprint and mismatch, optional Git metadata, unique dates/IDs, category coverage, label provenance/cross-references, one-to-one aliases, canonical story-family member Event IDs and exact unordered `distinct_material_development` pairs, rejection of response-local/cross-family/asymmetric/transitively inferred labels, Recall@10, actual-count Top 3 Precision, Duplicate Story Rate, claim-inventory factual/causal denominator enumeration, unsupported/overclaim numerators, complete unique audit coverage, `0/0 not_applicable`, summed aggregates, fixed permutations, v1 zero-adverse-delta offline gates, explicit baseline/tolerance version updates, and invalid denominator handling.
- [x] 11.2 Implement versioned evaluation schemas, label matching, metric oracles, deterministic correctness gates, and JSON/Markdown report models until the small fixtures pass.
- [x] 11.3 Define curation provenance/review rules and add the first ten unique ordinary and macro-policy trading-day fixtures.
- [x] 11.4 Add and independently validate ten unique company, China policy, and China-market trading-day fixtures.
- [x] 11.5 Add and independently validate ten unique China-US policy, geopolitical, abnormal cross-asset, and degraded-provider trading-day fixtures; validate the complete at-least-30-day coverage matrix.
- [x] 11.6 Implement the offline runner over fixed provider/market inputs with producer provenance and recorded four-pass outputs, including self-contained replay from exact saved Feed/effective config, exact correctness gates, per-day drill-downs, and failure on invalid fixtures before scoring.
- [x] 11.7 Write fake-client tests for opt-in live evaluation repetition counts, baseline selection, invocation-global request/time/cost budgets, mandatory concurrency 1 and stable day/repetition/pass/object/logical-attempt admission including transient retry order, application/prompt/model fingerprints, required exact-model local versioned price-table source/effective-date/fingerprint and returned-model allowset, missing/mismatched price exit 2, Decimal USD-per-million input/output rates, atomic pre-dispatch one-token-per-complete-request-byte plus max-output worst-case debit, cost immediately below/equal/above remaining budget, confirmed pre-send failure release, post-send connection loss/timeout and every HTTP-429/5xx envelope including usage-like fields retaining full reservation, retry admission against retained spend and request/time budgets, Responses-object exact-model/zero-reasoning/trustworthy-usage reconciliation without cache discount, model/reasoning/missing/invalid/over-reservation failure retaining reservation plus `budget_integrity_failure`, monotonic-time admission, local response storage with API `store: false`, refusal/failure counts, early budget exhaustion with `incomplete` status and exit 1, complete success exit 0, distributions, and absence from credential-free CI or runtime price fetching.
- [x] 11.8 Implement credentialed prompt/model comparison mode and concise evidence reports without claiming live bit-for-bit repeatability or silently treating incomplete runs as complete.

## 12. Workflows, Documentation, and Final Verification

- [x] 12.1 Write workflow-contract tests for clean install, offline-only CI, explicit permissions, non-cancelling Feed concurrency, no secret output, explicit Feed staging, artifact retention on publication failure, absent and explicit-false Feed opt-in values preventing checkout, provider work, credential access, and output-root access, an explicitly true value permitting work only after dedicated-runner/persistence/rate-state capability checks, and scheduled-Feed rejection on an ephemeral/fresh output root, missing persistence marker, unavailable durable rate state, destructive checkout cleanup, or a non-dedicated runner label.
- [x] 12.2 Add `.github/workflows/test.yml` for hosted credential-free CI and a `.github/workflows/generate-feed.yml` template scheduled near 00:20 UTC whose job has an explicit repository/environment opt-in gate defaulting false before checkout or provider work, requires the documented dedicated self-hosted label and explicit persistent shared output root when enabled, disables destructive cleanup of that root, validates its persistence marker/rate-state capability before requests, uses scoped permissions and tested concurrency, performs one staged generation followed by schema validation, explicitly stages only the run-scoped dated/latest paths, and exposes Git publication failure; do not set the gate, provision the runner/volume/repository/credentials, or grant write permission during Apply.
- [x] 12.3 Run workflow-equivalent commands locally against deterministic provider and LLM fixtures and validate both YAML files and referenced entry points; remote branch-protection behavior remains a documented deployment check.
- [x] 12.4 Complete `docs/architecture.md`, `docs/feed-contract.md`, `docs/scoring.md`, `docs/evaluation.md`, and provider contract notes, including the normative formulas, cutoff/time model, failure state machines, source lineage, scoring mappings, audit bundles, and offline/live evaluation split.
- [x] 12.5 Complete English and Chinese runbooks for configuration, Feed generation, full/degraded Brief execution, replay, tests, evaluations, GitHub deployment including the dedicated self-hosted persistent-output-root/rate-state prerequisite and unsupported cross-root boundary, the durable output-root registry and `initializing|active` recovery/deletion-corruption/policy-migration operational contract, OpenAI credentials, optional providers, external scheduling after successful Feed publication, and known limitations.
- [x] 12.6 Run the full unit/integration/schema-negative/security suite, all 30+ unique-day offline evaluations, deterministic correctness gates, and one credential-free Feed fixture proving every mandatory coverage-matrix capability/minimum; record exact commands and output without requiring a live endpoint or credential.
- [x] 12.7 Run an end-to-end mocked four-pass Brief plus deterministic-degraded generation and verify cutoff, provenance, identity, scoring, sparse behavior, rendering, audit bundle/replay, claim validation, and safety constraints.
- [x] 12.8 Run `openspec validate implement-follow-the-money-repository --strict`, broader strict validation, and `openspec doctor`; compare the final non-Change inventory with the recorded baseline (and inspect Git diff only if a separately initialized checkout exists), verify no unrelated files or secrets changed, and leave the Change ready for independent review.

## 13. Post-Apply Remediation and Independent Verification Gates

> The checked tasks above record the prior Apply claim. The 2026-08-11 independent review found that the production paths and acceptance evidence do not substantiate that claim. This Change remains incomplete until every gate below passes against the current stable revision. These gates do not replace the underlying requirements or permit weakening their tests.

- [x] 13.1 Provider and Feed production gate: complete and verify every mandatory provider contract and adapter required by the six-row coverage matrix; enable only verified implementations; connect the real registry, output-root collection lock, durable rate-state debit/reconcile lifecycle, bounded HTTP clients, stable concurrency joins, deadlines, schema validation, and monotonic publication to `follow-the-money feed`; prove a credential-free fixture-backed CLI run produces a valid healthy or explicitly degraded Feed rather than `no provider enabled`.
- [x] 13.2 Feed publication recovery gate: add behavior-based failure-injection and two-process tests for lock-before-cutoff planning, Asia/Shanghai dated paths, atomic no-replace dated commit, monotonic latest ownership, every required file/directory `fsync` boundary, `commit_durability_unknown`, crash recovery, idempotency, equal-cutoff variant ordering, deadline reserve, and zero mutation after cancellation; remove placeholder or source-text-only tests that do not execute publication behavior.
- [x] 13.3 Normal Brief production gate: replace `_mock_*`, empty editor output, and fixture-only shortcuts in the normal path with the complete deterministic Feed-to-ledger/candidate/event/packet/analytics/scoring/selection pipeline plus the resolver, analyst, editor, and language-audit Responses API passes; prove all ownership, reference, timeout/refusal/incomplete/partial-failure, ordering, deadline, audit, and no-fallback contracts through injected fake-client integration tests. `--degraded-report` remains a separate explicitly requested path and MUST NOT satisfy this gate.
- [x] 13.4 Bundle and replay gate: write the complete create-only atomic bundle with exact canonical Feed/effective config, real schema/config/prompt/model/build fingerprints, events, ledger, verified packets, structured LLM outcomes, analytics, selection, Brief/degraded object, rendered bytes, claim inventory, audits, and generation status; make `replay` re-execute and compare the full deterministic recorded-clock pipeline without network or LLM access, and fail on any member, identity, build, schema, config, reference, or output drift.
- [x] 13.5 Regression-evaluation gate: add at least 30 actual unique-date provenance-reviewed Feed fixtures and their referenced resolver/analyst/editor/audit outputs and labels; validate every referenced file and cross-reference before scoring; make offline CLI evaluation replay the fixtures, execute correctness/stability/quality gates, emit machine- and human-readable reports, and fail when a referenced fixture is absent or tampered instead of accepting a manifest-only count.
- [x] 13.6 Workflow gate: validate both GitHub Actions workflows with an executable workflow-aware validator and behavior-oriented contract tests; ensure the default-false gate prevents checkout/provider/credential/output-root access, persistent runner/rate-state checks precede sends, and the successful Feed run's exact run-scoped dated artifact plus latest artifact are mapped from the configured persistent output root into the explicitly staged repository paths without broad staging or hidden publication success.
- [x] 13.7 Repository quality gate: configure or document the project-applicable type-check command, then make the complete stable revision pass unit, integration, regression, schema-negative, security, workflow, CLI smoke, lint, format, type-check, and build checks with no skipped or weakened checks. Record exact commands, exit codes, test counts, and evaluation counts after the final relevant change.
- [x] 13.8 Final OpenSpec and scope gate: run `openspec validate implement-follow-the-money-repository --strict`, `openspec validate --all --strict`, and `openspec doctor`; record that `openspec verify` is unavailable if the installed CLI still returns `error: unknown command 'verify'`, use the project Verify gates from 13.1-13.7 instead, compare the final non-Change inventory with the recorded baseline, inspect Git diff only if a separately initialized checkout exists, and confirm no unrelated files or secrets changed.
- [x] 13.9 Fresh independent review gate: rebuild the complete requirement -> design -> implementation -> test -> validation-evidence matrix from current artifacts and code after 13.1-13.8 are stable; confirm every historical task is substantively complete, no normal production path contains mock/placeholder/test-only behavior, no Blocker or High finding remains, and only then mark this Change ready to archive.
