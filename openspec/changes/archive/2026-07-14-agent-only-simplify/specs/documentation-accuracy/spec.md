# documentation-accuracy Specification

## REMOVED Requirements

### Requirement: README 🅱️ local mode states JSON output, not markdown rendering
**Reason**: The 🅱️ local-mode section of `README.md` and `scripts/print.js` are both removed in this change. The requirement constrains documentation that no longer exists, so it is now a stale contract pointing at deleted content.
**Migration**: N/A — 🅱️ local mode is removed; the 🅰️ agent-mode documentation (which describes the LLM rendering `prompts/` templates) remains and is unaffected by this requirement's removal.
