## 1. Readiness and per-send managed transport

- [x] 1.1 Run `uv sync --frozen --all-groups` and `openspec validate eco-124-semantic-current-change-evidence --strict`; verify the frozen environment resolves and the Change passes before editing implementation code.
- [x] 1.2 Add failing `tests/test_provider_session.py` cases for two-send/two-debit behavior, deadline expiry before the second send, global and target-host limits per send, exactly-once numeric/HTTP-date `Retry-After` reconciliation with injected clocks, transport-failure reconciliation, pre-send refund, bounded manual redirects, unsafe/cyclic redirects, and unchanged single-request behavior. Add `bounded_fetch` integration cases proving typed `FetchError` metadata survives, `RateStateError` remains a hard execution failure, last concrete response time survives later no-response failure, and successful-resource progress survives retries; verify failures identify the missing boundaries.
- [x] 1.3 Create `src/follow_the_money/providers/session.py` with the narrow `ManagedProviderClient.get()`/`close()` boundary, manual redirect hops, exact resolved user-agent enforcement, dynamic target-host admission, and sole per-send rate transitions; verify `tests/test_provider_session.py` passes.
- [x] 1.4 Refactor `src/follow_the_money/feed/cli.py` so production clients receive shared scope locks, semaphores, clocks, cancellation, and deadline while `_run_adapter()` retains Provider-level retries/normalization/outcomes but performs no debit/refund/reconcile. Permit narrow `providers/http.py` typed-error passthrough, acquisition-progress metadata, and shared deterministic Retry-After/URL helper changes. Map terminal access denial after partial resources to existing serialized partial/blocked state even with zero accepted items, retain actual last-response time, let a complete retry recover its unit, and stop later company units after terminal incompleteness. Verify first-resource denial remains exempt, redirect-only denial remains exempt, later-resource denial is non-exempt across retries, counters are not fabricated, and the two focused test files pass.
- [x] 1.5 Review Task 1 for exactly one rate authority, one reconciliation per dispatched send, no hidden redirect send, preserved typed outcomes, and no semantic-domain changes; record an independent Task 1 commit after the focused tests pass.

## 2. Backward-readable v4 semantic contract

- [x] 2.1 Add failing compatibility and fail-closed tests in `tests/test_config.py`, `tests/test_feed_boundary.py`, and `tests/test_feed_bundle.py` proving old v4 SEC/CFTC provider-v1 payloads validate, SEC/CFTC v2 payloads missing semantics fail, complete v2 payloads validate, unknown semantic fields fail, unsupported Provider/version pairs fail, and new production remains Feed major 4.
- [x] 2.2 Extend `schemas/feed.schema.json` with closed additive SEC 13F and CFTC COT structures, SEC current/previous value-normalization descriptors and previous filing date, and bounded nullable/numeric forms while retaining legacy requirements; verify unknown and malformed fields fail without rejecting valid v1 payloads.
- [x] 2.3 Replace the global manifest-version equality check with an explicit Provider-specific supported-version map in `src/follow_the_money/providers/manifest.py`, retain `ProviderEntry.contract_version` in `config/model.py`, and update tests to prove SEC/CFTC support only versions 1/2 while all other Providers support only version 1.
- [x] 2.4 Add embedded Provider-version dispatch and v2 semantic invariants to `src/follow_the_money/feed/validate.py`, including status/null combinations, canonical units/numerics, ordered unique keys, derivation equations, and prohibited interpretation. Parse and validate the closed SEC v2 manifest unit entries and require value-normalization descriptors to match current/previous official filing dates; reject missing/unknown/changed unit entries before Provider work or durable mutation. Verify Task 2 tests using test-local v2 contracts pass.
- [x] 2.5 Keep all checked-in production manifests at version 1 during compatibility work. Use test-local SEC/CFTC v2 manifests to prove resolution and embedding under Feed schema v4. Defer the SEC production bump to Task 4.7 and CFTC bump to Task 5.7; verify this intermediate commit still produces payloads matching its advertised v1 contracts.
- [x] 2.6 Review Task 2 for bounded old-v4 read compatibility, fail-closed test-local v2 contracts, no hash-selected behavior, and no Feed-v5 or new domain; record an independent runnable Task 2 commit after focused tests pass. Production v2 readiness is reviewed only at each adapter activation commit.

## 3. Pure deterministic SEC 13F core

- [x] 3.1 Create failing `tests/test_sec_13f.py` coverage for exact-form cutoff selection by acceptance timestamp, distinct-report-period previous selection, amendment exclusion, unavailable previous behavior, complete-submission INFORMATION TABLE extraction, malformed/ambiguous tables, and deterministic filing ordering; verify failures identify the missing pure core.
- [x] 3.2 Use the official rule evidence in `references/provider-source-verification.md` to add failing SEC tests for both filing-date unit regimes, the 2023-01-03 boundary, a late filing for an older report period, exact USD / 1000 conversion, and a cross-transition comparison. Add missing/conflicting filing-date/unit authority failures, raw/converted/aggregate/delta numeric bound failures and no-rounding tests, alongside canonical security keys, duplicate aggregation, row-order independence, display selection, optional/conflicting FIGI, and nonnegative source values. Label synthetic fixtures truthfully; do not call them recorded official filings.
- [x] 3.3 Add failing comparison tests proving amount—not market value—selects increased/decreased/unchanged, matched deltas are reproducible, current-only is `new` with null previous/delta, previous-only is `no_longer_reported` with null current/delta, and no previous filing leaves previous/delta/change type null rather than `new`.
- [x] 3.4 Create `src/follow_the_money/providers/sec_13f.py` containing only Provider-specific pure selection, SGML/XML parsing, normalization, aggregation, and comparison functions or frozen value types; verify `.venv/bin/python -m pytest tests/test_sec_13f.py -q` passes without network access.
- [x] 3.5 Review Task 3 for no HTTP, no generic comparator/enrichment abstraction, exact `13F-HR` only, deterministic canonical-key ordering, and conservative missing semantics; record an independent Task 3 commit.

## 4. Complete SEC watched-company current-state slice

- [x] 4.1 Replace the single multi-CIK production adapter construction with one deterministic `SecEdgarAdapter` acquisition unit per resolved `WatchCompany`, all sharing `provider_id = sec_edgar`, one rate scope, and one `ProviderOutcome`; add tests proving eight configured companies remain one logical Provider outcome.
- [x] 4.2 Update `SecEdgarAdapter` so each acquisition attempt uses the managed client for submissions, current complete submission, and optional previous complete submission, validates official company identity and source URLs, delegates deterministic work to `sec_13f.py`, and raises typed failure for missing/ambiguous current evidence; verify each attempt performs no more than three logical resource GETs, with any redirect hops separately bounded and independently debited.
- [x] 4.3 Add production-shaped SEC fixtures for submissions plus current/previous complete submissions, including namespaces, duplicate rows, no-previous, malformed tables and matching/conflicting CIK/accession/form/FILED AS OF DATE. Verify the supported XML format and value mapping against official filing material; retain source URLs and distinguish recorded from synthetic fixtures. Prepare SEC v2 manifest unit entries and fixture provenance citing the verified rule, with activation deferred to Task 4.7. Stop activation if actual supported source shape contradicts the specified unit/date contract.
- [x] 4.4 Embed the resolved watched-company selection in `feed_config.snapshot` in deterministic CIK/ticker order. Validate SEC v2 exact CIK-set completeness before selection and on complete final outcomes; always validate each retained item and uniqueness/config membership. Test that blocked-exempt SEC has zero items and may publish degraded, a non-exempt incomplete diagnostic candidate may contain a valid subset only with pipeline failure/not_evaluated, and complete empty watched selection is permitted empty/no_snapshot. Preserve the v1 reader and reject malformed complete slices.
- [x] 4.5 Add failing snapshot/integration regressions for prior SEC A-H/current changed-A plus B-H, prior A-H/current unchanged A-G after removing H, watched-set addition, empty watched-set removal, and alias-only config changes. Implement the single complete-state current/prior identity-set equality carry predicate in `feed/snapshot.py`, admitted only for resolved SEC/CFTC v2 contracts; production CFTC remains v1 until Task 5.7. Verify changed slices replace wholly, equal-set identical bytes may carry, removed companies never return, and all v1/other six Provider subset and empty-check behavior remains unchanged.
- [x] 4.6 Add SEC integration tests for first-resource 401/403 versus submissions success followed by current/previous INFORMATION TABLE 401/403, including zero accepted items, prior successful company evidence, and resource progress across unsuccessful retries. Verify later denial is partial/blocked, non-exempt, pipeline-failing and not_evaluated; first-resource denial alone remains exempt without prior carry. Test terminal company failure cannot be cleared by later success; parser/current-selection/missing-table failure cannot publish, and no previous comparable filing remains publishable with unavailable comparison.
- [x] 4.7 Activate `providers/sec_edgar/manifest.yaml` contract version 2 with its verified units/provenance, complete SEC adapter, watched snapshot, completeness validation and exact-set carry gate in one working integration commit; leave CFTC v1. Run `.venv/bin/python -m pytest tests/test_sec_13f.py tests/test_adapters.py tests/test_feed_cli.py tests/test_feed_boundary.py tests/test_feed_bundle.py -q`. Verify advertised versions match working producers and no per-company merge or legacy semantic regression exists before committing.

## 5. Deterministic CFTC COT current/change evidence

- [x] 5.1 Create failing `tests/test_cftc_cot.py` cases for cutoff-aware latest/previous date selection, conservative publication boundary, market-code matching, display-name changes, row-order independence, current-only previous-unavailable semantics, omission of previous-only markets, numeric parse failure, duplicate/ambiguous codes, and deterministic output ordering.
- [x] 5.2 Add failing CFTC arithmetic tests for typed long, short, spreading, open interest, net `long - short`, every current-minus-previous delta, reproducible net change, canonical contract units, and rejection of directional/signal fields.
- [x] 5.3 Create `src/follow_the_money/providers/cftc_cot.py` with only Provider-specific pure report selection, row normalization, matching, derivation, and comparison; verify `.venv/bin/python -m pytest tests/test_cftc_cot.py -q` passes without network access.
- [x] 5.4 Replace CFTC's unqualified `$limit=100` fetch with managed date discovery and fixed-size sequential page acquisition for both dates, including total SoQL ordering, stable-row tie-breaker, date pinning, repeated/invalid page detection and closed termination bounds. Verify every page is separately managed. Add first-resource 401/403 versus discovery/page success followed by 401/403 tests, including zero normalized items and prior-attempt progress; only the former is eligible for exemption, while incomplete pagination fails closed without prior fallback.
- [x] 5.5 Prepare CFTC v2 pagination, `empty_valid_for_window = false`, identity and fixture provenance declarations and complete current/previous fixtures; verify official query/row shape, authoritative declarations and actual adapter behavior agree. Stage the production manifest changes only with the working adapter activation in Task 5.7, not in a standalone prematurely advertised contract commit.
- [x] 5.6 Update `CftcAdapter` to retain legacy `instrument_id`, `as_of`, and current-long `position` while publishing typed market identity, metrics, comparison, deltas, and derivation fields with official source provenance; verify previous-only markets are not synthesized.
- [x] 5.7 Activate `providers/cftc/manifest.yaml` contract version 2, pagination/identity/provenance, the working paginated semantic adapter and shared complete-state carry gate in one integration commit. Add the same-report-date correction regression: prior markets X/Y, complete corrected current X with byte-identical X, and no carried Y; keep zero-row reports failing. Run `.venv/bin/python -m pytest tests/test_cftc_cot.py tests/test_adapters.py tests/test_cftc_activation.py tests/test_feed_cli.py tests/test_feed_boundary.py tests/test_feed_bundle.py -q`. Verify both SEC/CFTC now produce v2 under Feed major 4, the other six Providers remain v1, acquisition is complete/cutoff-safe, and no interpretation is introduced before committing.

## 6. Feed identity, determinism, freshness, and snapshot integration

- [x] 6.1 Add identity tests that independently mutate 13F amount/change type/report period/current or previous filing date/value-normalization descriptors, watched selection, and CFTC source metrics/comparison date/deltas; verify canonical semantic projection, `content_digest`, and `run_id` change at a fixed cutoff. Separate raw projection mutation checks from validation tests so inconsistent arithmetic is never presented as a valid Feed.
- [x] 6.2 Add determinism tests permuting SEC rows, CFTC rows/pages, company adapter completion order, and Provider completion order; verify identical evidence yields identical typed output, canonical bytes, digest, and run ID.
- [x] 6.3 Add whole-slice lifecycle tests proving complete equal-set identical SEC-v2 slices produce byte-preserving valid_unchanged carry, watched-set removal/addition replaces the whole current slice, and empty selection removes prior SEC evidence. Verify partial/failed acquisition remains not_evaluated without fallback, genuine blocked exemption remains degraded with no SEC items, v1-to-v2 never relabels old payloads, and a complete same-date corrected CFTC v2 market set replaces rather than restores removed markets. All legacy event-list/empty-check behavior outside the narrow SEC/CFTC-v2 gate remains unchanged.
- [x] 6.4 Add freshness and cutoff tests proving SEC v2 current filings may predate `window.start` but must have precise acceptance before cutoff, CFTC post-cutoff publication is excluded, and original current/previous source times survive publication and carry-forward.
- [x] 6.5 Add a consumer-contract test that inspects one validated SEC item and one validated CFTC item without network, historical Feed, or `raw_metadata`, and verifies current state, previous/unavailable state, deterministic change, and evidence/derivation support are all available.
- [x] 6.6 Run `.venv/bin/python -m pytest tests/test_feed_determinism.py tests/test_feed_bundle.py tests/test_feed_boundary.py tests/test_feed_freshness.py tests/test_feed_cli.py tests/test_cftc_activation.py -q`, review all shared trust invariants and bounded size/numeric behavior, and record an independent Task 6 commit.

## 7. Contract, documentation, and final verification

- [x] 7.1 Update only truthful affected documentation in `references/feed-contract.md`, `references/architecture-boundary.md`, and `README.md`; update `SKILL.md` only if its current validated-Feed consumption instructions require the typed fields, and verify no document adds historical lookup, financial interpretation, or Digest editorial redesign.
- [x] 7.2 Reconcile the accepted `openspec/specs/feed-evidence-pipeline/spec.md` only through the repository's OpenSpec sync/archive phase rather than manually duplicating authority during implementation; verify the active delta remains the sole unarchived contract change and archived changes are untouched.
- [x] 7.3 Run `uv sync --frozen --all-groups` and every focused command named above, then run `.venv/bin/python scripts/quality_gate.py`; record only checks actually executed and resolve every failure without expanding ECO-124 scope.
- [x] 7.4 Run `openspec doctor`, `openspec validate eco-124-semantic-current-change-evidence --strict`, and `openspec validate --all --strict`; verify all commands pass before declaring implementation complete.
- [x] 7.5 Perform Review A (scope): verify only SEC 13F-HR and CFTC Legacy Futures-Only vertical slices were implemented, with no ECO-125 generic framework or later-issue capability.
- [x] 7.6 Perform Review B (trust contract): verify rate safety, deadline, redirect/host policy, typed hard failures, truthful response observation, partial-acquisition blocked exemption, filing-date unit authority, SEC/CFTC-v2 exact-set carry admission, cutoff, provenance, freshness, coverage, canonical identity, publication, and fail-closed behavior. Confirm the scoped snapshot admission correction is the only legacy selection change.
- [x] 7.7 Perform Review C (consumer contract): inspect one final validated 13F item and one CFTC item using no network/history/raw metadata, verify each answers current/previous/change/support without financial judgment, and record any unresolved conflict or risk honestly in the completion report.

## Delivery note

Tasks 1.5, 2.6, 3.5, 4.7, 5.7 and 6.6 required recorded independent per-task commits, and 4.7/5.7 required each Provider manifest activation to land in one working integration commit. That history was not created: the implementation was delivered and verified as one revision. Reproducing it after the fact by splitting the finished tree would fabricate non-building intermediate commits, because `providers/adapters.py` carries both the SEC and CFTC slices and `feed/cli.py` carries both the send-level transport move and the SEC watched-company acquisition, so it is recorded here as a deviation instead. Each of those tasks is otherwise complete. Post-implementation verification found and fixed two Blocker defects; their durable records are the production row-shape and value-unit evidence in `references/provider-source-verification.md`, the per-row source-URL rule in design Decision 9, and the regression tests named in `tests/`.
