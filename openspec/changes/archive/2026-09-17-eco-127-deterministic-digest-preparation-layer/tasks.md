## 1. Characterize the New Boundary

- [x] 1.1 Add failing tests in `tests/test_digest_prepare.py` for immutable code-level `DigestContext` types, `context_version = 1`, exact Feed identity/window binding, and byte-identical canonical JSON from repeated preparation of the same valid Feed; run `.venv/bin/python -m pytest tests/test_digest_prepare.py -q` and verify failure is caused only by the absent preparation module.
- [x] 1.2 Add failing domain-projection cases using valid `news`, `macro_release`, `policy`, `positioning`, and all three `filing` subtype fixtures; require fixed five-domain order, preserved Feed item order, exact item accounting, retained provenance/lineage and typed qualifiers, preserved null/absent/unavailable states, and exclusion of `raw_metadata` and representative schema-valid but ECO-126-unlisted fields; verify the focused cases fail on the missing projectors.
- [x] 1.3 Add failing parity tests that compare the implementation-declared eligible path inventory with `references/digest/domains/*.md`, including common provenance, optional `semantic_context`, positioning comparison/derivation fields, and filing subtype paths; verify a field present on only one side produces a focused failure rather than silently broadening or narrowing Agent evidence.
- [x] 1.4 Add failing status/coverage tests for healthy, blocked-degraded, carried/unchanged, valid-empty, and structured coverage-gap Feeds; require exact status and warnings, all Provider availability/freshness limitations in validated order, and zero-inclusive domain totals, then verify the focused cases fail before implementation.
- [x] 1.5 Extend `tests/test_feed_prepare.py` and `tests/test_no_llm_contract.py` with failing invocation regressions requiring canonical `DigestContext` stdout, exact existing retrieval/validation failures with no partial stdout, degraded warnings present in structured context, no persistent context file, and no model/prompt/renderer/orchestration imports; verify failures are limited to the unimplemented ECO-127 boundary.

## 2. Implement Typed Deterministic Preparation

- [x] 2.1 Create `src/follow_the_money/digest.py` with frozen, slotted context/status/Provider/domain/item types, the fixed v1 mapping projection, defensive freezing for nested evidence, a preparation-specific typed error, and canonical serialization through `canonical_bytes()`; verify the type/version, immutability, Feed-binding, and repeated-byte tests from 1.1 pass and `.venv/bin/mypy src/follow_the_money/digest.py` succeeds.
- [x] 2.2 Implement the explicit pipeline/status and Provider projectors in `src/follow_the_money/digest.py`, copying only accepted status, warning, coverage-gap, completion, availability, affected-group, count, and freshness fields from the validated Feed without current-config or wall-clock reads; verify all focused cases from 1.4 pass.
- [x] 2.3 Implement common identity/provenance plus `news`, `macro_release`, and `policy` projectors with exact ECO-126 field eligibility, optional matching `semantic_context`, and missing-evidence preservation; verify their domain and parity cases from 1.2-1.3 pass without parsing Markdown or `raw_metadata` at runtime.
- [x] 2.4 Implement `positioning` and the single subtype-aware `filing` projector for `form13f`, `form4`, and `beneficial_ownership`, retaining typed values together with their units/status/reasons/derivations/source references and rejecting no valid optional omission; verify the remaining domain and parity cases from 1.2-1.3 pass.
- [x] 2.5 Implement the private pure validated-Feed projection and the supported preparation entry that calls `consume_published_feed()` exactly once, accounts for every item under its validated `payload.type`, and produces no partial context on invariant failure; verify `.venv/bin/python -m pytest tests/test_digest_prepare.py tests/test_feed_remote.py -q` passes and existing remote Feed consumption remains unchanged.

## 3. Migrate Skill Invocation

- [x] 3.1 Add the preparation CLI behavior in `src/follow_the_money/digest.py` and change `scripts/skill/prepare-feed` to invoke it, preserving `prepare-feed:` retrieval/validation error text, exit codes, no-fallback behavior, and warning diagnostics while emitting only canonical v1 `DigestContext` JSON on success; verify the invocation cases from 1.5 pass.
- [x] 3.2 Update existing `tests/test_feed_prepare.py` expectations from raw logical Feed stdout to the exact context mapping while retaining the symlinked-install, missing-virtualenv, help, degraded-warning, and typed-failure regressions; run `.venv/bin/python -m pytest tests/test_feed_prepare.py tests/test_digest_prepare.py tests/test_no_llm_contract.py -q` and verify all pass.

## 4. Align Presentation and Current Documentation

- [x] 4.1 Update `references/digest/presentation-contract.md`, `references/digest/compression.md`, and only the directly affected `references/digest/domains/*.md` wording so closed field selection is preparation-owned while recommended representation, semantic-support assessment, consolidation/omission choices, compression accounting, and prose remain Host-Agent-owned; verify `.venv/bin/python -m pytest tests/test_digest_presentation_contract.py tests/test_digest_prepare.py -q` passes and the eligible-path parity remains exact.
- [x] 4.2 Update `SKILL.md`, `references/feed-contract.md`, `references/architecture-boundary.md`, `references/safety-boundary.md`, `docs/architecture.md`, `docs/feed-contract.md`, and current README/runbook wording only where the raw-Feed-to-Agent topology is stated; require the truthful `validated Feed -> DigestContext -> Host Agent -> evidence-preserving Digest` flow and explicit non-persisted/no-independent-evidence-schema boundary, then run `.venv/bin/python -m pytest tests/test_feed_documentation.py tests/test_digest_presentation_contract.py -q`.
- [x] 4.3 Audit `schemas/`, Provider/configuration files, publication code, generated `feeds/`, checkpoints, and runtime-state paths for accidental ECO-127 changes; verify `git diff --name-only` contains none of those surfaces and focused Feed bundle/remote/identity tests demonstrate unchanged Feed artifacts and validation authority.

## 5. Complete Verification

- [x] 5.1 Run `uv sync --frozen --all-groups` and verify the locked Python 3.12 development environment resolves without dependency or lockfile changes.
- [x] 5.2 Run `.venv/bin/python -m pytest tests/test_digest_prepare.py tests/test_feed_prepare.py tests/test_digest_presentation_contract.py tests/test_feed_documentation.py tests/test_feed_remote.py tests/test_feed_bundle.py tests/test_no_llm_contract.py -q` and resolve only ECO-127 regressions until the focused suite passes.
- [x] 5.3 Run `.venv/bin/python scripts/quality_gate.py` and verify the canonical repository gate passes with unchanged five-domain/eight-Provider Feed, deterministic identity, provenance, freshness, coverage, publication, evidence-only, credential-free, and no-model guarantees.
- [x] 5.4 Run `openspec doctor`, `openspec validate eco-127-deterministic-digest-preparation-layer --strict`, and `openspec validate --all --strict`; verify all succeed and the proposal, specs, design, tasks, implementation boundary, tests, and truthful current documentation agree.
