## 1. Regression tests first (pin current contract before the fix)

- [x] 1.1 In `tests/test_sec_13f.py`, add a production-shaped regression test: a synthetic complete submission modeled on the observed Oaktree shape (SEC header, primary `13F-HR` `<XML>` block with an `edgarSubmission` root using a default namespace, an `INFORMATION TABLE` `<XML>` block with `<ns1:informationTable xmlns:ns1="http://www.sec.gov/edgar/document/thirteenf/informationtable">`, an `<?xml ...?>` declaration with leading whitespace inside the block, and at least one prefixed `<ns1:infoTable>` holding). Comment it as a synthetic production-shaped regression derived from the namespace shape observed in SEC 13F complete submissions, not an official filing copy. Verify it currently fails with the exactly-one error before the fix (red), and that it passes afterwards
- [x] 1.2 Add cases covering arbitrary prefix (`<sec:informationTable xmlns:sec="...">`), prefixed child elements round-tripped into holdings (cusip, amounts, value, issuer name), and a complete submission where the primary `edgarSubmission` document and the prefixed information table coexist (only the information table is selected). Verify by running `.venv/bin/python -m pytest tests/test_sec_13f.py` and observing the new cases red before the fix, green after
- [x] 1.3 Add cases pinning unchanged fail-closed behavior under the new recognition: two information-table-rooted documents (any namespace form) still fail with the exactly-one error; a truncated `<XML><informationTable><infoTable>` block still fails with the malformed error; a standalone prefixed information table (no `<XML>` wrapper, no header) parses; default-namespace and no-namespace inputs still parse. Verify with `.venv/bin/python -m pytest tests/test_sec_13f.py`

## 2. Implementation (single module, single function)

- [x] 2.1 Rework `_extract_document()` in `src/follow_the_money/providers/sec_13f.py` per design.md Decisions 1-6: keep regex `<XML>...</XML>` block extraction; when no blocks exist, treat the whole text as a single candidate (standalone fallback); for each candidate, `.strip()` then `ET.fromstring` — on ParseError raise the existing malformed SchemaError only if the unparseable text contains `informationTable` (case-insensitive), otherwise skip the block; classify parsed documents by root local name `informationTable` (exact case); require exactly one classified table or raise the existing exactly-one SchemaError; return the parsed root element. Verify `.venv/bin/python -m pytest tests/test_sec_13f.py` is fully green
- [x] 2.2 Confirm no other call sites or behavior depend on the old string-list shape of `_extract_document()` (its only caller is `parse_complete_submission()` in the same module) and that `_local()`/`_text()` required no changes. Verify by grep for `_extract_document` across `src/` and by the holdings assertions in the new prefixed-child test

## 3. Quality gates and OpenSpec verification

- [x] 3.1 Run the canonical quality gate `.venv/bin/python scripts/quality_gate.py` (covers pytest, ruff, mypy per repo convention) and confirm it passes
- [x] 3.2 Run `openspec validate fix-sec-13f-namespaced-information-table --strict` and `openspec doctor`, and confirm both pass; then run the change verification workflow against the delta spec scenarios before archiving
