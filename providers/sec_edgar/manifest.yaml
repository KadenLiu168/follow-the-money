# SEC EDGAR — provider contract manifest
# Contract status: verified on 2026-08-11 against the public submissions JSON
# contract and the checked-in fixture.

contract_version: 1
provider_id: sec_edgar
name: SEC EDGAR
source_family_id: sec
tier: "Tier 1"
verification:
  verified: true
  verification_date: "2026-08-11"
  contract_url: "https://www.sec.gov/cgi-bin/browse-edgar?action=getcompany&type=13F"
  usage_note: >
    Company filings (13F/13D/G, Form 4, etc.) for watched companies. Public
    data; EDGAR requires a descriptive user agent with a contact identifier.
    The submissions endpoint and archive source-link rules are verified.

authentication: none
protocol: https
user_agent: "follow-the-money/0.1 kaden@example.com"

charset:
  allowed: [utf-8]
  bom_allowed: false
  content_type_header: null

fetch_hosts:
  - { host: www.sec.gov, allow_subdomains: false, allowed_ports: [443] }
  - { host: data.sec.gov, allow_subdomains: false, allowed_ports: [443] }
redirect_hosts:
  - { host: www.sec.gov, allow_subdomains: false, allowed_ports: [443] }
  - { host: data.sec.gov, allow_subdomains: false, allowed_ports: [443] }

source_link_hosts:
  - host: sec.gov
    allow_subdomains: true
    allowed_ports: [443]
    allowed_query_params: []
    query_value_grammar: any
    drop_query_params: []
  - host: www.sec.gov
    allow_subdomains: false
    allowed_ports: [443]
    allowed_query_params: []
    query_value_grammar: any
    drop_query_params: []

rate_policy:
  scope_id: sec_edgar
  capacity: 10
  refill_period_seconds: 60
  minimum_interval_seconds: 5

response_limit_bytes: 10485760
attempt_timeout_seconds: 20

time:
  knowledge_time: acceptance_publication_instant
  payload_types: [filing]

identity:
  stable_record_id: accession_number

units: {}

freshness:
  policy: filing_acceptance_within_window

empty_valid_for_window: true
pagination: page_number
default_enabled: true

fixture_provenance:
  source: "Recorded from live responses on 2026-08-11; fixture-backed adapter contract."
  files:
    - fixtures/browse-13f.json
