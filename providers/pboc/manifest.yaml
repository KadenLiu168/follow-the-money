# PBOC (中国人民银行) — provider contract manifest
# Contract status: re-verified on 2026-09-20 against the recorded public HTML
# index and the recorded official announcement page. Mandatory
# china_official_macro_policy.

contract_version: 2
provider_id: pboc
name: 中国人民银行
source_family_id: pboc
tier: "Tier 1"
verification:
  verified: true
  verification_date: "2026-09-20"
  contract_url: "https://www.pbc.gov.cn/goutongjiaoliu/113456/113469/index.html"
  usage_note: >
    Official policy announcements and monetary-policy news from pbc.gov.cn.
    Public data, no authentication; the HTML index, the #r_con .newslist_style
    candidate list, and the verified article container (div#zoom) were
    re-verified on 2026-09-20.

authentication: none
protocol: https
user_agent: "follow-the-money/0.1 (+https://github.com/KadenLiu168/follow-the-money)"

charset:
  allowed: [utf-8]
  bom_allowed: false
  content_type_header: null

fetch_hosts:
  - { host: pbc.gov.cn, allow_subdomains: false, allowed_ports: [443] }
  - { host: www.pbc.gov.cn, allow_subdomains: false, allowed_ports: [443] }
redirect_hosts:
  - { host: pbc.gov.cn, allow_subdomains: false, allowed_ports: [443] }

source_link_hosts:
  - host: pbc.gov.cn
    allow_subdomains: true
    allowed_ports: [443]
    allowed_query_params: []
    query_value_grammar: any
    drop_query_params: []

rate_policy:
  scope_id: china_gov
  capacity: 20
  refill_period_seconds: 60
  minimum_interval_seconds: 1

response_limit_bytes: 10485760
attempt_timeout_seconds: 20

time:
  knowledge_time: published_at
  payload_types: [policy]

identity:
  stable_record_id: announcement_id_or_url

units:
  percent: percent
  bps: bps

freshness:
  cadence: event_driven
  reference_time: checked_at

empty_valid_for_window: true
pagination: none
default_enabled: true

fixture_provenance:
  source: "Recorded verbatim from live official responses on 2026-09-20; fixture-backed adapter contract."
  files:
    - fixtures/index.html
    - fixtures/detail-2026091815494839605.html

content:
  acquisition: detail_document
  extraction_method: official_html_text_v1
  allowed_content_types: [text/html]
  max_document_bytes: 2097152
  max_text_chars: 12000
  max_detail_documents_per_window: 50
  required_for_selected_item: true
